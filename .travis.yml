language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-urania' ]; then cd .. && eval "mv $currentfolder generator-urania" && cd generator-urania; fi

